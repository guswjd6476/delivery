'use client';

import LoginForm from '../../components/LoginForm';

const LoginPage = () => {
    return (
        <div className="p-4">
            <h1 className="text-2xl mb-4">로그인</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
